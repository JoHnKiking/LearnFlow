# 复制为 .env：cp .env.example .env
# Access denied / using password: YES → DB_PASSWORD 必须与 MySQL 里该用户密码一致；
# 终端能 `mysql -u root -p` 登录的密码，原样写入 DB_PASSWORD（无密码则留空或删除该行）。

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Wzy283537!
DB_NAME=learnflow

JWT_SECRET=change-me
JWT_EXPIRES_IN=7d
REFRESH_TOKEN_EXPIRES_IN=30d

LLM_PROVIDER=volcano
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=
