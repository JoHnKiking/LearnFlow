# 用法：在 mobile 目录执行
#   cp .env.example .env
# 然后只改 .env（已 git 忽略）。改完需重启 Metro（Ctrl+C 后再 npx expo start）。
#
# 前提：本机已跑 API（如 cd server && npm run dev），且 ngrok 指向同一端口：
#   ngrok http 3001
#
# 地址 = ngrok 界面里 Forwarding 的 https 那一串，后面拼上 /api（不要末尾多一个 /）
# 免费版每次新开 ngrok，子域可能变，变了就更新 .env 里这一行。
EXPO_PUBLIC_API_URL=https://heterotrichous-gerty-catadromous.ngrok-free.dev/api
