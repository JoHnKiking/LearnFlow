# 数据库连接（与主服务共用同一个 MySQL）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Wzy283537!
DB_NAME=learnflow

# 监控服务端口（独立于主服务的 3001）
MONITOR_PORT=3002

# 访问看板的 API Key
MONITOR_API_KEY=change-me-monitor-key

# 快照采集间隔（分钟），默认 5
SNAPSHOT_INTERVAL_MINUTES=5
